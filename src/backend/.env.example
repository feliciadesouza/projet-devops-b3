NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=diabetetrack
DB_USER=postgres
DB_PASSWORD=postgres

JWT_SECRET=change_me_in_production

# Origines CORS (séparées par des virgules). En AWS, Terraform ajoute l’URL du frontend S3.
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost